# Root Environment Variables Example
# This file shows all the environment variables needed across all components
# Copy the specific env.example files from each component directory

# =============================================================================
# AGENT SERVER (.env)
# =============================================================================
# Copy agent-server/env.example to agent-server/.env

# =============================================================================
# JOB RUNNER (.env)
# =============================================================================
# Copy job-runner/env.example to job-runner/.env

# =============================================================================
# FRONTEND (.env.local)
# =============================================================================
# Copy frontend/env.example to frontend/.env.local

# =============================================================================
# SEI-JS (.env)
# =============================================================================
# Copy sei-js/env.example to sei-js/.env

# =============================================================================
# COMMON VARIABLES (can be set at root level)
# =============================================================================

# Development Environment
NODE_ENV=development
PYTHON_ENV=development

# Ports (if not specified in component env files)
AGENT_SERVER_PORT=8000
JOB_RUNNER_PORT=3000
FRONTEND_PORT=3001
MCP_SERVER_PORT=8080

# Logging
LOG_LEVEL=info

# Docker (if using docker-compose)
DOCKER_COMPOSE_PROJECT_NAME=sei-hackathon 